var newString = 'I am a "double quoted" string inside quotes';
console.log(newString);

var newString = `I am a "double quoted" string along with 'single quoted string'`;
console.log(newString);

//Here \ is an escape character
var newString = "I am a \"double quoted\" string inside \"double quotes\"";
console.log(newString);